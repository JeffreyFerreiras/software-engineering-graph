interface:
  display_name: "Create Pull Request"
  short_description: "Create GitHub pull requests"
  default_prompt: "Use $create-pull-request to create a review-ready GitHub pull request from the selected changes."
